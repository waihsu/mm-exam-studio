{
  "name": "mm-exam-studio",
  "version": "0.5.1",
  "description": "Open-source exam practice and question-paper workspace for Myanmar learners.",
  "author": "MM Exam Studio contributors",
  "license": "MIT",
  "packageManager": "bun@1.2.4",
  "workspaces": [
    "./server",
    "./admin",
    "./client",
    "./shared"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:client": "turbo dev --filter=client",
    "dev:admin": "turbo dev --filter=admin",
    "dev:server": "turbo dev --filter=server",
    "build": "turbo build",
    "build:client": "turbo build --filter=client",
    "build:admin": "turbo build --filter=admin",
    "build:server": "turbo build --filter=server",
    "cf:whoami": "bunx wrangler whoami",
    "cf:deploy:client": "cd client && bun run build && bunx wrangler pages deploy dist --project-name examstudio --commit-dirty=true",
    "cf:deploy:admin": "cd admin && bun run build && bunx wrangler pages deploy dist --project-name adminstudio --commit-dirty=true",
    "cf:deploy:api": "cd server && bun run cf:deploy:pdf && bun run cf:deploy",
    "cf:deploy:frontends": "bun run cf:deploy:client && bun run cf:deploy:admin",
    "cf:deploy": "bun run cf:deploy:api && bun run cf:deploy:frontends",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "verify:release": "bun run build && bun run lint && turbo run test --filter=admin --filter=server --filter=shared && cd server && bun run pdf:smoke",
    "postinstall": "turbo build --filter=shared --filter=server"
  },
  "keywords": [
    "education",
    "exam-practice",
    "question-papers",
    "myanmar",
    "open-source"
  ],
  "devDependencies": {
    "bun-types": "latest",
    "turbo": "^2.6.3",
    "wrangler": "^4.38.0"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "better-auth": "^1.5.5"
  }
}
